# Baud rate for the switch CPU console
CONSOLE_BAUD_RATE=115200

# Flow control: 0=disabled, 1=enabled (default: 0)
CONSOLE_FLOW_CONTROL=0

# Remote device name shown in consutil
CONSOLE_REMOTE_DEVICE=SwitchCpu
