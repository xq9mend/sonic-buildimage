#
#  /etc/rsyslog.conf    Configuration file for rsyslog.
#
#                       For more information see
#                       /usr/share/doc/rsyslog-doc/html/rsyslog_conf.html
#
# /etc/rsyslog.conf for the SONiC gNMI/telemetry container family.
# Derived from dockers/docker-base-trixie/etc/rsyslog.conf. The only differences are in
# remote syslog forwarding: RELP/2514 with UDP/514 fallback for hosts that only listen
# on UDP/514, and the non-blocking buffering moved from a per-action queue to the main
# queue so the forwarding actions stay queue-less.


#################
#### MODULES ####
#################

#
# Set a rate limit on messages from the container
#
module(load="imuxsock" SysSock.RateLimit.Interval="300" SysSock.RateLimit.Burst="20000") # provides support for local system logging
#module(load="imklog")   # provides kernel logging support
#module(load="immark")  # provides --MARK-- message capability

# provides UDP syslog reception
#module(load="imudp")
#input(type="imudp" port="514")

# provides TCP syslog reception
#module(load="imtcp")
#input(type="imtcp" port="514")


###########################
#### GLOBAL DIRECTIVES ####
###########################

set $.CONTAINER_NAME=getenv("CONTAINER_NAME");

# Keep container syslog writers non-blocking if host-side syslog backpressure fills the
# queue. Dropping at queue saturation is safer than stalling service initialization in
# syslog()/sendto(). Applied at the main queue so the forwarding actions stay queue-less,
# which the RELP->UDP failover below requires.
main_queue(queue.type="LinkedList" queue.size="20000" queue.timeoutEnqueue="0")

# Set remote syslog server.
# RELP/2514 is preferred; UDP/514 is a fallback so the image keeps forwarding on hosts
# that only listen on UDP/514. The omrelp action suspends on first failure
# (action.resumeRetryCount="0") and the omfwd action runs only while omrelp is suspended
# (action.execOnlyWhenPreviousIsSuspended="on").
#
# Neither forwarding action carries a per-action queue: a per-action queue defers the
# send to a worker thread, so the suspended flag is set asynchronously and the omfwd
# action sees stale state. Non-blocking behavior under backpressure comes from the
# main_queue settings above instead.
template (name="ForwardFormatInContainer" type="string" string="<%PRI%>%TIMESTAMP:::date-rfc3339% %HOSTNAME% %$.CONTAINER_NAME%#%syslogtag%%msg:::sp-if-no-1st-sp%%msg%")
module(load="omrelp")
*.* action(type="omrelp" target=`echo $SYSLOG_TARGET_IP` port="2514" action.resumeRetryCount="0" action.resumeInterval="30" Template="ForwardFormatInContainer")
*.* action(type="omfwd" target=`echo $SYSLOG_TARGET_IP` port="514" protocol="udp" action.execOnlyWhenPreviousIsSuspended="on" Template="ForwardFormatInContainer")

#
# Use traditional timestamp format.
# To enable high precision timestamps, comment out the following line.
#
#$ActionFileDefaultTemplate RSYSLOG_TraditionalFileFormat

# Define a custom template
$template SONiCFileFormat,"%TIMESTAMP%.%timestamp:::date-subseconds% %HOSTNAME% %syslogseverity-text:::uppercase% %$.CONTAINER_NAME%#%syslogtag%%msg:::sp-if-no-1st-sp%%msg:::drop-last-lf%\n"
$ActionFileDefaultTemplate SONiCFileFormat

#
# Set the default permissions for all log files.
#
$FileOwner root
$FileGroup adm
$FileCreateMode 0640
$DirCreateMode 0755
$Umask 0022

#
# Where to place spool and state files
#
$WorkDirectory /var/spool/rsyslog

#
# Include all config files in /etc/rsyslog.d/
#
$IncludeConfig /etc/rsyslog.d/*.conf

#
# Suppress duplicate messages and report "message repeated n times"
#
$RepeatedMsgReduction on

###############
#### RULES ####
###############
